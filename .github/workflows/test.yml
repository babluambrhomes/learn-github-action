name: my first action

on:
    push:
        branches:
            - main

jobs:
    hello:
        runs-on: ubuntu-latest

        steps:
            - name: say hello
              run: echo "hello from git hub action"