export default function Contact() {
  return (
    <section id="contact" className="bg-zinc-900 py-24 text-white">
      <div className="mx-auto max-w-3xl px-6 text-center">
        <h2 className="text-3xl font-bold tracking-tight">
          Ready to get started?
        </h2>
        <p className="mx-auto mt-4 max-w-md text-zinc-400">
          Join thousands of happy users building with us today. It takes less
          than a minute to sign up.
        </p>
        <button className="mt-8 rounded-full bg-white px-8 py-3 text-sm font-semibold text-zinc-900 transition-colors hover:bg-zinc-200">
          Create free account
        </button>
      </div>
    </section>
  );
}